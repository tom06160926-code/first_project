# JWT 配置
# 生产环境请使用强随机密钥，可以使用 python -c "import secrets; print(secrets.token_urlsafe(32))" 生成
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-environment

# 数据库配置
DATABASE_URL=sqlite:///../database/books.db

# Token 过期时间（小时）
JWT_EXPIRATION_HOURS=24
